language: php
php:
    - 5.2
    - 5.3
    - 5.4
compiler:
    - gcc
    - clang
before_script:
    #    - sudo ln -sf /opt/phpfarm/inst/bin/php-config-* /usr/bin/php-config
    - sudo apt-get install libgtest-dev swig
    - which php-config
    - ls -l /home/travis/.phpenv/bin
script: scons test

